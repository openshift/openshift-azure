apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicecatalog-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicecatalog-api
  template:
    metadata:
      labels:
        app: servicecatalog-api
    spec:
      containers:
      - name: apiserver
        image: {{ .Values.serviceCatalogImage }}
        command:
        - /usr/bin/service-catalog
        args:
        - apiserver
        - --storage-type
        - etcd
        - --secure-port
        - "6443"
        - --etcd-servers
        - https://master-etcd:2379
        - --etcd-cafile
        - /var/run/kubernetes-service-catalog/etcd-ca.crt
        - --etcd-certfile
        - /var/run/kubernetes-service-catalog/etcd-client.crt
        - --etcd-keyfile
        - /var/run/kubernetes-service-catalog/etcd-client.key
        - -v
        - "3"
        - --cors-allowed-origins
        - localhost
        - --enable-admission-plugins
        - KubernetesNamespaceLifecycle,DefaultServicePlan,ServiceBindingsLifecycle,ServicePlanChangeValidator,BrokerAuthSarCheck
        - --feature-gates
        - OriginatingIdentity=true
        volumeMounts:
        - name: config
          mountPath: /var/run/kubernetes-service-catalog
          readOnly: true
      volumes:
      - name: config
        secret:
          secretName: servicecatalog-api
