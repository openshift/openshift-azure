apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: openshift-console
  name: console
  namespace: openshift-console
spec:
  host: console.apps.107.22.59.203.xip.io
  port:
    targetPort: https
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: reencrypt
  to:
    kind: Service
    name: console
    weight: 100
  wildcardPolicy: None
