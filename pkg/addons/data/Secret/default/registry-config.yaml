apiVersion: v1
kind: Secret
metadata:
  name: registry-config
  namespace: default
type: Opaque
stringData:
  config.yml: |
    version: 0.1
    log:
      level: debug
    http:
      addr: :5000
    storage:
      delete:
        enabled: true
      cache:
        blobdescriptor: inmemory
      azure:
        accountname: '*** GENERATED ***'
        accountkey: REGISTRY_STORAGE_ACCOUNT_KEY
        container: registry
        realm: core.windows.net
    auth:
      openshift:
        realm: openshift
    middleware:
      registry:
      - name: openshift
      repository:
      - name: openshift
        options:
          pullthrough: True
          acceptschema2: True
          enforcequota: False
      storage:
      - name: openshift
