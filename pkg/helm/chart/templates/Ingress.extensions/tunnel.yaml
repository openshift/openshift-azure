apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: tunnel
  annotations:
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/secure-backends: "true"
spec:
  tls:
  - hosts:
    - {{ .Values.TunnelHostname | quote }}
  rules:
  - host: {{ .Values.TunnelHostname | quote }}
    http:
      paths:
      - backend:
          serviceName: tunnel
          servicePort: 444
