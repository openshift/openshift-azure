package validate

import (
	acsapi "github.com/openshift/openshift-azure/pkg/api"
)

// ContainerService validates a ContainerService struct
func ContainerService(new, old *acsapi.ContainerService) (errs []error) {
	return
}
